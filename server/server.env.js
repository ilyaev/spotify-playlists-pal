const clientId = '[CLIENT_ID]' // update to actual
const clientSecret = '[CLIENT_SECRET]' // update to actual
const redirectUrl = 'http://127.0.0.1/'

module.exports = { clientId, clientSecret, redirectUrl }
